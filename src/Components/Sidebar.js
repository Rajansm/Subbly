import { Container } from "react-bootstrap";

const Sidebar = () => {
  return (
    <Container fluid>
      <h4>This will contain sidebar elements</h4>
    </Container>
  );
};

export default Sidebar;
